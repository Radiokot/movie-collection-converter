package ua.com.radiokot.mcc.cli

import com.github.ajalt.clikt.core.CliktCommand

class Mcc: CliktCommand(help = "An utility that converts one movie collection to another") {
    override fun run() = Unit
}